name: forms-flow-rsbcservice-CI

on:
  workflow_dispatch:
  push:
    branches-ignore:
      - main
      - main-rsbc
      - develop
    paths:
      - "forms-flow-rsbcservice/**"

jobs:
  ci:
    uses: ./.github/workflows/ci-workflow.yml
    with:
      working_directory: ./forms-flow-rsbcservice
      node_version: '20.x'
      enable_lint: false
      enable_test: true
      enable_build: true

